public class module01 {
    // java is a platform independent language, it just needs JVM to run
    // JVM(java virtual machine) is available for multiple OS but not iPhone lol
    // JVM understands only byte code, but we create Java Code which is converted to byte code by the compiler
    // JVM needs the first file that needs to be executed which must have a "main" method
    // A class is must for java code execution
    // JVM is a part of JRE(Java Runtime Environment)
    // JRE is a part of JDK(Java Development Kit)
    public static void main(String[] args) {
        System.out.println("This is the beginning!");
    }
}